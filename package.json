{
  "name": "my-apollo-fetcher",
  "version": "1.0.0",
  "description": "apollo httpLink fetcher for ali app",
  "main": "build",
  "repository": "git@github.com:kdong007/ali-apollo-fetcher.git",
  "keywords": [
    "支付宝小程序",
    "apollo",
    "graphql"
  ],
  "license": "MIT",
  "scripts": {
    "build": "babel index.js -d build --presets=env"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "babel-preset-env": "^1.7.0",
    "eslint": "^5.6.1"
  }
}
